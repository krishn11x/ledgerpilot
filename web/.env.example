# Frontend env template -- COMMITTED. No secrets here.
# Vite only exposes variables prefixed with VITE_ to the browser, so anything
# put here is public by definition. Never add an API key.
#
#   cp .env.example .env.local

# Leave blank to use the Vite dev proxy at /api (recommended for local dev).
VITE_API_BASE_URL=
